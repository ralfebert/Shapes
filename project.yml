name: Shapes
options:
  bundleIdPrefix: com.example
  generateEmptyDirectories: true
  deploymentTarget:
    iOS: "13.0"
targets:
  Shapes:
    type: application
    platform: iOS
    sources:
      - path: Shapes
        excludes:
          - "Assets/*.sketch"
    settings:
      base:
        SWIFT_VERSION: 5.1